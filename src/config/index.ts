import type { ControlOptions } from "../types";

export const DEFAULT_CONTROLS: ControlOptions = {
  showRemoved: false,
  showPrivate: true,
  showPlanned: false,
  scaleBySize: true,
  showLabels: false,
};
